# Server-side only — never exposed to the browser.
# Fine-grained PAT with public-repo read access. Effectively required:
# the dashboard makes ~30 GitHub calls per 5-minute refresh (~360/hr worst case),
# far above the 60/hr unauthenticated limit. Without it, automated cards
# degrade to "Unknown" with a rate-limit note.
GITHUB_TOKEN=
